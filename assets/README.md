# assets/

`demo.gif` is generated automatically by:

```bash
npm run demo:gif
```

The same GIF is also written to `public/demo.gif`, which the root `README.md` embeds.
Intermediate capture frames live in `demo_frames/` (gitignored).
